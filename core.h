#ifndef HYPERSET_H
#define HYPERSET_H

#include <string>
#include <vector>
#include "hyperset.h"

namespace hyperset {

using namespace std;
using namespace boost;

const string AND      = "and";
const string OR       = "or";
const string ONLY     = "only";

} // namespace
#endif
